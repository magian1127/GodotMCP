# 复制为 .env（被 git 忽略）并填入本机实际值；脚本按"显式参数 > .env"解析。
# 只放**无法从脚本位置推导**的仓库外机器事实；任何凭据(token/密码/API key)都不得写入本文件。
# 发布/多机使用提示：DSH 接入层无需任何配置，直接把 DSH 指向
# <仓库>\adapters\dsh\deepseek-harness-godot_unified（或用 dsh-godot install --link）。

# Godot 4.x 可执行文件：装在哪只有本机知道，脚本无法推导。
# 使用者：install-godot-project.ps1、test-mcp-config-discovery.ps1
GODOT_EXECUTABLE=<Godot 可执行文件>
