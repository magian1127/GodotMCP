<?xml version="1.0" encoding="utf-8"?>
<!-- self-contained 单文件发布(spec US21 / issue 03 验收):
     dotnet publish src/godot-mcp-daemon -c Release -p:PublishProfile=win-x64
     产物:server-dotnet/publish/win-x64/godot-mcp-daemon.exe -->
<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <PublishDir>..\..\publish\win-x64\</PublishDir>
  </PropertyGroup>
</Project>
